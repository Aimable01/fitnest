import 'package:flutter/material.dart';

class Swipe1 extends StatelessWidget {
  const Swipe1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Swipe 1 screen'),
      ),
    );
  }
}
