import 'package:flutter/material.dart';

class Swipe2 extends StatelessWidget {
  const Swipe2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Swipe 2 screen')),
    );
  }
}
